server:
  port: 10001

spring:
  application:
    name: config-server
  #配置读取本地文件
  profiles:
    active: native
  security:
    user:
      name: roy
      password: 123456

management:
  security:
    enabled: true