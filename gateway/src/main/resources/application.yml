server:
  port: 8765
spring:
  application:
    name: gateway
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8760/eureka/
zuul:
  routes:
    api:
      path: /api/**
      serviceId: api
    api2:
      path: /api2/**
      serviceId: api2
