#注册中心
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:10000/eureka/

#配置中心
spring:
  cloud:
    config:
      fail-fast: true
      label: master
      profile: dev
      discovery:
        enabled: true
        service-id: config-server
      #配置配置中心访问密码
      username: roy
      password: 123456