#注册中心
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:10000/eureka/

#配置中心
spring:
  cloud:
    config:
      fail-fast: true
      label: master
      profile: dev
      discovery:
        enabled: true
        service-id: server_config_center
      #配置中心认证信息
      name: roy
      password: GTI5FHvSYBf2c=
  #链路追踪
  zipkin:
    baseUrl: http://localhost:9411
  sleuth:
    traceId128: true
    sampler:
      probability: 1.0