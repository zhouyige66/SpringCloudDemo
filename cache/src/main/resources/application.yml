server:
  port: 10004

spring:
  application:
    name: cache
  cache:
    ehcache:
      config: classpath:ehcache.xml
    redis:
      cache-null-values: true
  redis:
    host: localhost
    port: 6379